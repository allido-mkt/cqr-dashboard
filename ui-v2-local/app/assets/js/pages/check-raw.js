import { APP_CONFIG } from "../config.js";
import { getState, setFilter, setRawCheck } from "../state.js";
import { callAuthorized } from "../services/admin-api.js";
import { icon, statusPill, escapeHtml, showToast, optionMarkup } from "../ui.js";

const ACTIVE_KEY="cqr_raw_check_active_request_id";
const HISTORY_KEY="cqr_raw_check_browser_history";
let pollTimer=null,polling=false;
const specificGames=APP_CONFIG.games.filter(i=>i.value!=="ALL");
function readHistory(){try{return JSON.parse(localStorage.getItem(HISTORY_KEY)||"[]");}catch{return [];}}
function writeHistory(row){const rows=[row,...readHistory().filter(x=>x.requestId!==row.requestId)].slice(0,10);localStorage.setItem(HISTORY_KEY,JSON.stringify(rows));}
function tone(status){return ["completed","raw_ready"].includes(status)?"ready":["failed","raw_missing"].includes(status)?"danger":["running","queued"].includes(status)?status:"warning";}
function progress(raw){if(raw.status==="completed"||raw.status==="failed")return 100;if(raw.totalJobs)return Math.round(((raw.completedJobs+raw.failedJobs+raw.runningJobs*.5)/raw.totalJobs)*100);return raw.status==="running"?50:raw.status==="queued"?15:0;}
function renderJobs(raw){const jobs=raw.jobs||[];return jobs.length?`<div class="table-wrap"><table><thead><tr><th>Game</th><th>Period</th><th>Status</th><th>Result</th><th>Tabs</th><th>Raw Hash</th></tr></thead><tbody>${jobs.map(j=>`<tr><td>${escapeHtml(j.game_code)}</td><td>${escapeHtml(j.period_key)}</td><td>${statusPill(tone(j.status),escapeHtml(j.status))}</td><td>${escapeHtml(j.result_status||"-")}</td><td>${escapeHtml(j.tab_count_found??"-")} / ${escapeHtml(j.tab_count_expected??5)}</td><td class="code-chip">${escapeHtml(j.raw_data_hash||"-")}</td></tr>`).join("")}</tbody></table></div>`:"";}
export function renderCheckRawPage(){const state=getState(),r=state.rawCheck,history=readHistory();return `<div class="page-grid"><section class="grid-wide-aside"><article class="surface-card warm-card"><div class="card-header"><div><h2 class="card-title">Check Raw</h2><p class="card-description">ส่ง Request เข้า Queue แล้วติดตามสถานะอัตโนมัติทุกประมาณ 5 วินาที</p></div>${statusPill(tone(r.status),r.status==="idle"?"Ready":r.status)}</div><div class="card-body"><div class="form-grid two"><label class="form-field"><span class="form-label">Game</span><select class="form-control" id="raw-game">${optionMarkup(specificGames,state.filters.game==="ALL"?specificGames[0].value:state.filters.game)}</select></label><label class="form-field"><span class="form-label">Month</span><select class="form-control" id="raw-month">${optionMarkup(APP_CONFIG.months,state.filters.month==="ALL"?APP_CONFIG.months[0].value:state.filters.month)}</select></label></div><div class="toolbar" style="margin-top:14px"><button class="button primary" id="raw-submit" type="button" ${["queued","running"].includes(r.status)?"disabled":""}>${icon("play","nav-icon")} ${["queued","running"].includes(r.status)?"Request active":"Run Raw Check"}</button>${r.requestId?`<button class="button" id="raw-copy" type="button">${icon("copy","nav-icon")} Copy Request ID</button>`:""}</div><div class="notice warning" style="margin-top:14px">Manual Raw Check ต้องเลือก Game และ Month อย่างละ 1 ค่า ระบบไม่อนุญาต ALL</div></div></article><aside class="surface-card"><div class="card-header"><div><h2 class="card-title">Active Request</h2><p class="card-description">Reload หน้าแล้วระบบจะกลับมาติดตาม Request เดิม</p></div></div><div class="card-body"><div class="metric-grid"><div class="metric-box"><div class="metric-label">Request ID</div><div class="metric-value code-chip">${escapeHtml(r.requestId||"-")}</div></div><div class="metric-box"><div class="metric-label">Scope</div><div class="metric-value">${escapeHtml(r.game||"-")} / ${escapeHtml(r.month||"-")}</div></div><div class="metric-box"><div class="metric-label">Progress</div><div class="metric-value">${progress(r)}%</div></div><div class="metric-box"><div class="metric-label">Current Job</div><div class="metric-value">${escapeHtml(r.currentGame||"-")} ${escapeHtml(r.currentMonth||"")}</div></div></div></div></aside></section><article class="surface-card"><div class="card-header"><div><h2 class="card-title">Queue Status</h2><p class="card-description">Controller → Queue → Worker → Completed / Failed</p></div></div><div class="card-body"><div class="progress-track"><div class="progress-fill" style="width:${progress(r)}%"></div></div><div class="metric-grid four" style="margin-top:14px"><div class="metric-box"><div class="metric-label">Queued</div><div class="metric-value">${r.queuedJobs}</div></div><div class="metric-box"><div class="metric-label">Running</div><div class="metric-value">${r.runningJobs}</div></div><div class="metric-box"><div class="metric-label">Completed</div><div class="metric-value">${r.completedJobs}</div></div><div class="metric-box"><div class="metric-label">Failed</div><div class="metric-value">${r.failedJobs}</div></div></div>${r.errorMessage?`<div class="notice danger" style="margin-top:12px">${escapeHtml(r.errorMessage)}</div>`:""}${r.status==="completed"?`<div class="metric-grid four" style="margin-top:14px"><div class="metric-box"><div class="metric-label">Result</div><div class="metric-value">${escapeHtml(r.resultStatus||"-")}</div></div><div class="metric-box"><div class="metric-label">Tabs</div><div class="metric-value">${escapeHtml(r.tabsFound)} / ${escapeHtml(r.tabsExpected)}</div></div><div class="metric-box"><div class="metric-label">Missing Tabs</div><div class="metric-value">${escapeHtml(r.missingTabs||"-")}</div></div><div class="metric-box"><div class="metric-label">Raw Hash</div><div class="metric-value code-chip">${escapeHtml(r.rawHash||"-")}</div></div></div>`:""}${renderJobs(r)}</div></article><article class="surface-card"><div class="card-header"><div><h2 class="card-title">Recent Requests</h2><p class="card-description">ประวัติใน Browser นี้เท่านั้น ข้อมูลกลางอยู่ที่ RawCheckRequests / RawCheckJobs</p></div></div><div class="card-body">${history.length?`<div class="table-wrap"><table><thead><tr><th>Request ID</th><th>Scope</th><th>Status</th><th>Finished</th></tr></thead><tbody>${history.map(h=>`<tr><td class="code-chip">${escapeHtml(h.requestId)}</td><td>${escapeHtml(h.game)} / ${escapeHtml(h.month)}</td><td>${statusPill(tone(h.status),escapeHtml(h.status))}</td><td>${escapeHtml(h.finishedAt||"-")}</td></tr>`).join("")}</tbody></table></div>`:'<div class="empty-state">ยังไม่มีประวัติใน Browser นี้</div>'}</div></article></div>`;}
function applyStatus(result){const terminal=["completed","failed"].includes(String(result.status));const job=(result.jobs||[])[0]||{};setRawCheck({requestId:result.request_id||"",game:result.target_games_csv||result.current_game_code||getState().rawCheck.game,month:result.target_months_csv||result.current_period_key||getState().rawCheck.month,status:result.status||"queued",resultStatus:job.result_status||result.result_status||"",totalJobs:Number(result.total_jobs||0),queuedJobs:Number(result.queued_jobs||0),runningJobs:Number(result.running_jobs||0),completedJobs:Number(result.completed_jobs||0),failedJobs:Number(result.failed_jobs||0),currentGame:result.current_game_code||"",currentMonth:result.current_period_key||"",tabsFound:job.tab_count_found??"-",tabsExpected:job.tab_count_expected??5,missingTabs:job.missing_tabs||"",rawHash:job.raw_data_hash||"",finishedAt:result.finished_at||job.finished_at||"",errorMessage:result.error_message||job.error_message||"",jobs:result.jobs||[]});if(terminal){localStorage.removeItem(ACTIVE_KEY);writeHistory({requestId:result.request_id,game:result.target_games_csv,month:result.target_months_csv,status:result.status,finishedAt:result.finished_at||new Date().toISOString()});}}
async function poll(requestId){if(polling)return;polling=true;try{let result=await callAuthorized("admin.n8n.raw.status",{request_id:requestId});if(result.ok===false)throw new Error(result.message||"ไม่พบ Request");if(["completed","failed"].includes(result.status))result=await callAuthorized("admin.n8n.raw.status",{request_id:requestId,include_jobs:"true"});applyStatus(result);if(!["completed","failed"].includes(result.status))pollTimer=setTimeout(()=>poll(requestId),Math.max(1000,Number(result.poll_after_ms||5000)));}catch(error){setRawCheck({errorMessage:error.message||String(error)});if(/Session/i.test(error.message||""))return;pollTimer=setTimeout(()=>poll(requestId),5000);}finally{polling=false;}}
async function submit(){const game=document.getElementById("raw-game")?.value,month=document.getElementById("raw-month")?.value;if(!game||game==="ALL"||!month||month==="ALL"){showToast("เลือก Game และ Month อย่างละ 1 ค่า");return;}setFilter("game",game);setFilter("month",month);setRawCheck({game,month,status:"queued",errorMessage:"",queuedJobs:1,totalJobs:1});try{const result=await callAuthorized("admin.n8n.raw.check",{game,month,target_game_code:game,target_month:month,target_games_csv:game,target_months_csv:month,check_mode:"manual",expected_tabs_csv:"Registered,DAU,Returners,Late_Starters,Login"},60000);const payload=result.n8n_result||result;const requestId=payload.request_id||result.request_id;if(!requestId)throw new Error("Backend ไม่ได้ส่ง request_id กลับมา");localStorage.setItem(ACTIVE_KEY,requestId);setRawCheck({requestId,status:payload.status||"queued",totalJobs:Number(payload.total_jobs||1),queuedJobs:Number(payload.queued_jobs??1)});poll(requestId);}catch(error){localStorage.removeItem(ACTIVE_KEY);setRawCheck({status:"failed",failedJobs:1,errorMessage:error.message||String(error)});}}
export function bindCheckRawPage(){document.getElementById("raw-submit")?.addEventListener("click",submit);document.getElementById("raw-copy")?.addEventListener("click",()=>navigator.clipboard.writeText(getState().rawCheck.requestId).then(()=>showToast("Copied Request ID")));const active=localStorage.getItem(ACTIVE_KEY);if(active&&!getState().rawCheck.requestId){setRawCheck({requestId:active,status:"queued"});poll(active);}else if(active&&!["completed","failed"].includes(getState().rawCheck.status))poll(active);}
