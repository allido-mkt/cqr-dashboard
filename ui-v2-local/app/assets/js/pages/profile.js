import { APP_CONFIG } from "../config.js";
import { getState, setFilters } from "../state.js";
import { escapeHtml, statusPill, showToast, optionMarkup } from "../ui.js";

const PREF_KEY="cqr_user_preferences";
function prefs(){try{return JSON.parse(localStorage.getItem(PREF_KEY)||"{}")||{};}catch{return {};}}
export function renderProfilePage(){const {user}=getState(),expires=user.exp?new Date(user.exp*1000).toLocaleString("th-TH"):"-";return `<div class="page-grid"><article class="surface-card"><div class="card-body profile-page-card"><div class="profile-large-avatar">${escapeHtml(user.initials)}</div><div><h2 style="margin:0">${escapeHtml(user.displayName)}</h2><p style="margin:5px 0 0;color:var(--muted)">${escapeHtml(user.email)}</p><div class="toolbar" style="margin-top:12px">${statusPill("ready","Active")}${statusPill(user.roleId==="super_admin"?"ready":"warm",escapeHtml(user.roleId))}</div></div></div></article><section class="grid-2"><article class="surface-card"><div class="card-header"><div><h2 class="card-title">Account Information</h2><p class="card-description">Read-only source of truth from authenticated Backend session</p></div></div><div class="card-body form-grid"><label class="form-field"><span class="form-label">Display Name</span><input class="form-control" value="${escapeHtml(user.displayName)}" disabled></label><label class="form-field"><span class="form-label">Email</span><input class="form-control" value="${escapeHtml(user.email)}" disabled></label><label class="form-field"><span class="form-label">Role</span><input class="form-control" value="${escapeHtml(user.roleId)}" disabled></label><label class="form-field"><span class="form-label">Allowed Games</span><input class="form-control" value="${escapeHtml(user.allowedGames||"ALL")}" disabled></label><label class="form-field"><span class="form-label">Allowed Regions</span><input class="form-control" value="${escapeHtml(user.allowedRegions||"ALL")}" disabled></label></div></article><article class="surface-card"><div class="card-header"><div><h2 class="card-title">Session</h2><p class="card-description">ข้อมูล Session ปัจจุบัน</p></div></div><div class="card-body metric-grid"><div class="metric-box"><div class="metric-label">Expires</div><div class="metric-value">${escapeHtml(expires)}</div></div><div class="metric-box"><div class="metric-label">Permissions in UI</div><div class="metric-value">${user.permissions.length}</div></div></div></article></section></div>`;}
export function bindProfilePage(){}
export function renderPreferencesPage(){const p=prefs(),game=p.defaultGame||getState().filters.game,view=p.defaultView||getState().filters.periodType||"month";return `<article class="surface-card"><div class="card-header"><div><h2 class="card-title">User Preferences</h2><p class="card-description">ค่าที่เก็บเฉพาะ Browser นี้ ไม่รวม Webhook, Schedule หรือ Integration</p></div></div><div class="card-body preference-list"><div class="preference-row"><div><h4>Default Game</h4><p>เกมเริ่มต้นเมื่อเปิดหน้าใหม่</p></div><select id="pref-game" class="form-control">${optionMarkup(APP_CONFIG.games.filter(x=>x.value!=="ALL"),game)}</select></div><div class="preference-row"><div><h4>Default View</h4><p>Monthly หรือ Weekly</p></div><div class="segmented" id="pref-view"><button class="${view==="month"?"active":""}" data-view="month" type="button">Monthly</button><button class="${view==="week"?"active":""}" data-view="week" type="button">Weekly</button></div></div><div class="preference-row"><div><h4>Language</h4><p>UI V2 ยังไม่ได้แปลครบทุกหน้า จึงยังไม่เปิดสลับภาษา</p></div><input class="form-control" value="ไทย / English (mixed UI)" disabled></div><div class="preference-row"><div><h4>Compact Tables</h4><p>ลดความสูงของแถวตาราง</p></div><label class="checkbox-row"><input id="pref-compact" type="checkbox" ${p.compactTables?"checked":""}><span>Use compact rows</span></label></div><div class="toolbar"><button id="save-preferences" class="button primary" type="button">Save Preferences</button></div></div></article>`;}
export function bindPreferencesPage(){let selectedView=prefs().defaultView||getState().filters.periodType||"month";document.querySelectorAll("#pref-view button").forEach(b=>b.addEventListener("click",()=>{selectedView=b.dataset.view;document.querySelectorAll("#pref-view button").forEach(x=>x.classList.toggle("active",x===b));}));document.getElementById("save-preferences")?.addEventListener("click",()=>{const value={defaultGame:document.getElementById("pref-game").value,defaultView:selectedView,compactTables:document.getElementById("pref-compact").checked};localStorage.setItem(PREF_KEY,JSON.stringify(value));document.documentElement.classList.toggle("compact-tables",value.compactTables);setFilters({game:value.defaultGame,periodType:value.defaultView});showToast("Saved Preferences in this browser");});}
